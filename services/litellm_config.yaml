model_list:
  - model_name: deepseek-v4-flash    # 这个名称将在 Codex 中作为 --model 参数使用
    litellm_params:
      model: deepseek/deepseek-chat    # 指定使用 DeepSeek 的模型
      api_key: os.environ/DEEPSEEK_API_KEY
